{
  "name": "server-jwt-auth",
  "version": "1.0.0",
  "description": "json server auth with jwt",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "reda amine",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.2",
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0"
  }
}
